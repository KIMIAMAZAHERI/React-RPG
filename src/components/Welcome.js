//Landing page which will consist of:
//Header. 
//sign up button and log in button (both will open modals with small forms) - functionality not needed
//Main section: different choices - play, character selection, maybe stats.

//Make own components for sign up, log in and the main section? 

const Welcome = () => {
    return (
        <div>Welcome</div>
    )
}

export default Welcome;